{
  "name": "bangdd",
  "version": "0.3.0",
  "private": true,
  "description": "Adds a google bang in the duckduckgo search",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "lint": "eslint ."
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
